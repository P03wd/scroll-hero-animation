export const metadata = {
  title: "Scroll Animation",
  description: "Hero scroll animation project",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}